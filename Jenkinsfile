pipeline {
	agent any
	stages {

		stage('Build') {
			steps {
				echo 'maven clean test'
			}
		}

		stage('Test') {
			steps {
				echo "Test Jenkins"
			}
		}

		stage('Deploy') {
			steps {
				echo "Deploy Jenkins"
			}
		}
	}
}
