pipeline {
	agent any
	tool {
		maven
	}
	stages {

		stage('Build') {
			steps {
				echo "Build Jenkins"
			}
		}

		stage('Test') {
			steps {
				echo "Test Jenkins"
			}
		}

		stage('Deploy') {
			steps {
				echo "Deploy Jenkins"
			}
		}
	}
}
