pipeline {
	agent any
	stages {

		stage('Build') {
			steps {
				sh "mkdir lib"
			}
		}

		stage('Test') {
			steps {
				echo "Test Jenkins"
			}
		}

		stage('Deploy') {
			steps {
				echo "Deploy Jenkins"
			}
		}
	}
}
